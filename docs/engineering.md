<!-- arggon:generated template="docs/engineering.md" -->
# Engineering standards (suizo)

Project-owned review bar and definition of done. Keep this short, enforceable, and current — update it in the same PR that changes the standard.

## Review bar

A PR is reviewable when:

- [ ] It does one thing (small, focused diff).
- [ ] Tests cover the behavior change (new tests for new behavior, updated tests for changed behavior).
- [ ] Docs affected by the change are updated **in the same PR** (README, `docs/`, ADRs).
- [ ] The work item id from `tasks/` is referenced in the PR.
- [ ] `arggon validate` passes (if the change touches `tasks/` or templates).

A change is acceptable when it satisfies the bar above and a reviewer can answer:

- Would I have written it this way? (clarity, naming, no cleverness without cause)
- Is the failure mode handled (errors, empty states, concurrency)?
- Does it make the next change easier or harder?

## Definition of done

- [ ] Acceptance checklist in the work item body is complete.
- [ ] Tests green locally and in CI (including lint/typecheck gates).
- [ ] Docs updated in the same PR.
- [ ] PR merged with the work item id referenced.
- [ ] `arggon update <id> --status done` (never reopen afterwards).

## Testing expectations

<!-- Project-specific: coverage expectations, what needs integration vs unit tests, fixtures policy. -->

- **Unit vs integration split:** unit tests are table-driven and cover domain
  operations in `tournament.go` and persistence in `store.go` (in-memory, temp
  dirs, no network). The only "integration" surface is the CLI itself —
  `run()` is tested end-to-end against a temp store file with real exit codes.
- **Before merge:** every new behavior in a command or domain method ships with
  table cases covering the happy path, each validation error, and at least one
  boundary (empty, first element, large ids). Persistence changes must include
  a round-trip case. `go vet ./...`, `go test ./...` and `golangci-lint run`
  must be clean.

## ADRs

Cross-cutting decisions (stack, schema, new top-level package) get an ADR under `docs/adr/`: numbered, `Proposed` in the PR, `Accepted` on merge, superseded instead of rewritten.

---

Generated by `arggon init` 2026 — edit freely; `arggon init` never overwrites existing files.
